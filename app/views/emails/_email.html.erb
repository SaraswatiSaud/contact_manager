<table>
  <tr>
    <th>Email:</th>
    <td><%= email.address %></td>
    <td><%= link_to 'Edit', edit_person_email_path(@person, email) %></td>
    <td><%= link_to 'Delete', [email.person, email],
                    method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
</table>
